#include <stdio.h>

/**
 * main - A program that prints digit from zero to ten
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d\n", i);
	return (0);
}
