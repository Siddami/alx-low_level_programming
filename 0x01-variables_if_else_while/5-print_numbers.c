#include <stdio.h>

/**
 * main - A program that prints digit from 0 to 10
 *
 * Return: always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d\n", i);
	return (0);
}
